"""Evaluation harness: a versioned question set, an automated runner, and
timestamped results. Hits the real API and costs money, so it lives outside
``tests/`` and is run by hand (architecture doc section 4.7).
"""
