"""Feature + model layer. One subpackage per source that has a model, e.g.
``model.seattle_energy``. A source's ``model_module`` in its ``SourceSpec``
points here and enables the ``predict`` tool.
"""
