# Cloud Run image (milestone M7): Flask + agent + analytics.db baked in, mode=ro.
# Placeholder — see architecture doc section 10.
#
# FROM python:3.11-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# COPY analytics.db /app/analytics.db
# ENV DEPLOY_MODE=deployed ANALYST_MODEL=claude-haiku-4-5 MAX_AGENT_ITERS=6
# CMD ["gunicorn", "-b", "0.0.0.0:8080", "app.api:create_app()"]
