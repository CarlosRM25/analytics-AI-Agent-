# analytics-agent dependencies (architecture doc appendix).
# Unpinned for the M0/M1 scaffold; versions get locked once the agent loop lands (M4).

# --- ingestion + storage ---
requests
SQLAlchemy          # Core, over stdlib sqlite3
PyYAML              # catalog/*.yaml — column docs read by ingest + describe_schema
sqlparse            # run_sql guardrails (M4)

# --- modeling ---
pandas
scikit-learn
joblib

# --- agent ---
anthropic

# --- charts ---
plotly
kaleido             # static PNG export for notebooks (Plotly's image backend)

# --- notebooks (M2 EDA) ---
jupyterlab
nbconvert           # headless execute: jupyter nbconvert --execute
ipykernel

# --- interface ---
click
flask

# --- config ---
pydantic-settings
python-dotenv

# --- deploy: rate-limit + response cache ---
redis

# --- tooling ---
ruff
pytest
