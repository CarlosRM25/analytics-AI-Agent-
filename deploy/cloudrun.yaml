# Cloud Run service config (milestone M7). Placeholder — architecture doc section 10.
# Scales to zero; API key from Secret Manager (never in the image); CORS locked
# to the portfolio origin; GET /health for the uptime monitor.
